// this file contains correct input

struct Point {
    int x;
    int y;
};


bool fk () {
    return true;
}

int fn (int a, int b) {
    return a;
}

void fp() {
    struct Point p;
    cin >> p.x;
    cout << fn(p.x, p.y);
    return;
}

int main() {
    int x;
    int y;
    bool c;
    struct Point p;
    struct Point q;

    cin >> x;
    cout << x;

    p.x = x;
    p.y = y;

    fn(x, y);
    fp();

    while ( !fk() && y>2 ) { x++; }

    while ( true ) { x++; }
    while (c || x<2) { x++; }

    repeat (y)
    {int r_x; }

    while ( c == (x==y) ) { x++; }
    if ( y == x ) { x++; }
    if ( fn(p.x, p.y)== y ) { x++; }
    else { x = q.y;  x++; }

    if (x>1 && c) { x = 1; }

    return 0;
}